{{name}}
<div class="line__tooltip">
    {{#each data}}
        <div class="line__tooltip_label">
            <span class="label" style="background-color: {{color}}">{{num}}</span>
            <span class="line__tooltip_name">{{key}}</span>
        </div>
    {{/each}}
</div>
